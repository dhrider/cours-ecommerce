<div class="col">
    <div class="card bg-{{ color }} text-white text-center">
        <div class="card-body">
            <i class="fas fa-{{ icon }} fa-3x mb-3"></i>
            <p>{{text}}</p>
        </div>
    </div>
</div>